# Create a context page
Creates a new wiki page entry.

Provide the title, author and page content. You can include spaces in the content argument by wrapping it in quotes.

Run `.claude/scripts/context/create_page.sh "<title>" "<author>" "<content>"`
