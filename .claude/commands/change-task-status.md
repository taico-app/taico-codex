# Changes the status of a task
Enum of NOT_STARTED, IN_PROGRESS, FOR_REVIEW, DONE

Run `.claude/scripts/tasks/change_task_status.sh "<task id>" "<status>"`

Alternatively, you can use the MCP server tool `mcp__tasks__change_task_status` which provides the same functionality.