# Create task
Creates a task in our board that can be tracked and updated.

Give it a descriptive title.
Give it a detailed description with acceptance criteria.

Run `.claude/scripts/tasks/create_task.sh "<task title>" "<task description>"`

Alternatively, you can use the MCP server tool `mcp__tasks__create_task` which provides the same functionality.