import { Thread } from './types';

// Placeholder API service for threads
export class ThreadsService {
  static async listThreads(): Promise<{ items: Thread[] }> {
    // Placeholder - will be implemented when backend is ready
    return { items: [] };
  }
}
