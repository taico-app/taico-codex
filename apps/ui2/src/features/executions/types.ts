/**
 * Execution types for the UI
 * These match the v2 execution DTOs from the backend
 */
export type {
  TaskExecutionQueueEntryResponseDto,
  ActiveTaskExecutionResponseDto,
  TaskExecutionHistoryResponseDto,
} from "@taico/client/v2";
