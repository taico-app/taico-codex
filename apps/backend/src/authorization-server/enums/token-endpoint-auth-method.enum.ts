/**
 * OAuth 2.0 token endpoint authentication methods.
 * Defines how clients authenticate when requesting tokens.
 */
export enum TokenEndpointAuthMethod {
  NONE = 'none',
  CLIENT_SECRET_BASIC = 'client_secret_basic',
}
