apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-stack
  namespace: ai-stack
  annotations:
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - taico.PLACEHOLDER_DOMAIN
      secretName: ai-stack-tls
  rules:
    - host: taico.PLACEHOLDER_DOMAIN
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ai-stack
                port:
                  number: 80
