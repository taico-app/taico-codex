apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: taico-codex
  namespace: taico-codex
spec:
  dnsNames:
    - codex.taico.app
  secretName: taico-codex-tls
  issuerRef:
    name: letsencrypt-cluster-issuer
    kind: ClusterIssuer
